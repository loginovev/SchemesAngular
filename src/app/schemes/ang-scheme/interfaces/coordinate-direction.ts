import {enumDirection} from "../enum/enumDirection";

export interface CoordinateDirection{
    top:number;
    height:number;
    left:number;
    width:number;
    direction:enumDirection;
}
